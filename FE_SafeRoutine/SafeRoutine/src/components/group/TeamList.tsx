// import React from 'react';
// import GroupListBar from '../manage/GroupListBar'; // 경로는 실제 구조에 맞게 수정해 주세요
// import { GroupMemberProps } from '../group/Types'; // 경로는 실제 파일 위치에 맞게 조정해야 함

// interface TeamListProps {
//   data: GroupMemberProps[];
//   onSelectTeam: (teamId: number) => void;
// }

// const TeamList: React.FC<TeamListProps> = ({ data, onSelectTeam }) => {
//     const uniqueTeamIds = Array.from(new Set(data.map(item => item.teamId)));
  
//     return (
//       <GroupListBar teamIds={uniqueTeamIds} onSelectTeam={onSelectTeam} />
//     );
//   };

// export default TeamList;
